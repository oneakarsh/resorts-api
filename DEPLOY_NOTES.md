render-deployment branch
------------------------
1. Set up Supabase project
2. Copy "Transaction mode" connection string to DATABASE_URL in .env
3. Run: npx prisma migrate deploy
4. Deploy to Render
